#include <stdio.h>
int main(){
    int n,m,soma,multi,sub,divi;
    n = 20;
    m = 40;
    soma = n + m;
    multi = n*m;
    sub = n-m;
    divi = n/m;
    printf("A soma é %d, a multiplicacao é %d, a subtracao é %d, a divisao é %d",soma,multi,sub,divi);
}
